function save(){
    v1=contact.name.value
    v2=contact.email.value
    v3=contact.mess.value
    console.log(v1,v2,v3)
    if(v1 !="" && v2 !="" &&  v3!=""){
        alert("You will be  contacted shortly")
    }    
}